# JOURNAL

[2026-07-22T00:00:00] Démarrage CYCLE 11 → GOAL.md + CHECKPOINT.md créés, backlog analysé (ok)
[2026-07-22T00:50:00] Itération 1 : 2 layouts (Focus/Filmstrip) + noise gate AudioWorklet + caméra/écran simultanés (refonte senders vidéo voice.ts) + 2 bugfix latents (VoiceBar icône caméra, toggleVideo sender dupliqué) → tsc clean, cargo check clean, npm run build OK, déployé VPS (client dist resync propre), smoke test prod 200 sur /, bundle JS et noise-gate-worklet.js (ok, preuve : curl 200 sur les 3)
[2026-07-22T01:00:00] Traitement limitation connue : audio système du partage d'écran mixé au micro (Web Audio graph) au lieu d'être perdu/remplacer le micro → tsc clean, build OK, commit 8337d59 pushé forgejo+github (ok)
[2026-07-22T01:02:00] Découverte : CI Forgejo Actions (.forgejo/workflows/deploy.yml) déploie déjà automatiquement sur push main — scp manuel précédent redondant. Vérifié VPS HEAD=8337d59 après push (ok, preuve : git rev-parse sur VPS)
[2026-07-22T01:05:00] Momo se couche, demande "traite tout + crée une loop d'amélioration". CYCLE 11 explicite entièrement traité → GOAL.md/CHECKPOINT.md basculés en mode loop nocturne, bornée à 8 itérations, périmètre sûr défini (pas de DB/archi/prod)
[2026-07-22T01:10:00] Loop nocturne itération 1 : a11y switcher de disposition (aria-label + aria-pressed + role=group, 6 boutons icône-seule sans nom accessible) → tsc clean, build OK, commit 4b5055c pushé, CI Forgejo a déployé tout seul (vérifié : VPS HEAD=4b5055c) (ok)
[2026-07-22T01:28:00] Loop nocturne itération 2 : bug réel trouvé — peer qui rejoint PENDANT un partage d'écran avec audio système ne recevait que le micro brut (le mix n'était poussé qu'aux peers déjà connectés au moment du mix). Ajout _mixedAudioTrack traqué globalement + appliqué aux nouveaux _createPC → tsc clean, cargo check clean, build OK, commit 062f612 pushé (bundlé avec 2 commits markdown-only précédents non poussés), CI a déployé (vérifié : VPS HEAD=062f612) (ok)
[2026-07-22T01:57:00] Loop nocturne itération 3 : bug serveur réel — mark_all_read (reads.rs) ignorait les deux Result de tokio::join!, renvoyait {"ok":true} même si les requêtes DB échouaient. Propagation d'erreur ajoutée (?), cohérent avec le reste du fichier. cargo check : 9→7 warnings (les 2 unused-Result disparus). tsc clean (aucun changement client). Commit f41549f pushé, CI a rebuild le conteneur server Docker (première fois ce cycle qu'un changement server déclenche un vrai rebuild Rust) — attente ~2 min via poll conteneur, confirmé recréé + healthy + VPS HEAD=f41549f (ok, preuve : docker compose ps "healthy", git rev-parse)
[2026-07-22T02:27:00] Loop nocturne itération 4 : nettoyage mécanique — `cargo fix` a retiré 7 imports `require_member`/`require_channel_in_server` inutilisés (threads/polls/forum/scheduled/messages/uploads/saved.rs), cargo check 7→0 warnings. tsc clean, diffs vérifiés (uniquement les imports confirmés inutilisés retirés, rien d'utilisé touché). Commit f211f3e pushé, conteneur server rebuild+healthy confirmé, VPS HEAD=f211f3e (ok)
[2026-07-22T02:57:00] Loop nocturne itération 5 : a11y — Whiteboard (overlay plein écran, 5 boutons outils icône-seule sans nom accessible, pas de role dialog, pas d'Escape pour fermer) + Soundboard (bouton fermer icône-seule sans aria-label, slider volume sans label). Ajout aria-label/aria-pressed/role="dialog"/role="group" + handler Escape→onClose. Perf MessageList écarté (refactor trop large/risqué pour une nuit sans supervision — 1562 lignes, un seul composant monolithique, extraction en sous-composant memoïsé = trop de surface de risque). tsc clean, build OK, cargo check clean (sans changement). Commit e895ea8 pushé (client seul, déploiement rapide), VPS HEAD=e895ea8 confirmé (ok)
[2026-07-22T03:26:00] Loop nocturne itération 6 : audit a11y des autres modales (KeyboardShortcutsModal/CommandPalette/StickerPicker/GifPicker) → déjà toutes bien couvertes (role dialog, aria-label, hook useEscapeKey partagé), rien à corriger là. Audit systémique des autres tokio::join! serveur (9 sites) → tous déjà correctement gérés (bind + propagation ou dégradation gracieuse voulue), le bug de it.3 était isolé. `npx eslint src` (jamais lancé explicitement ce cycle malgré présence dans npm run build) → 2 warnings "unused eslint-disable directive" (MessageInput.tsx, ForumPage.tsx) — directives react-hooks/exhaustive-deps devenues inutiles, supprimées. eslint+tsc+build clean. Commit 9ac230d pushé (client seul), VPS HEAD=9ac230d confirmé (ok)
[2026-07-22T03:56:00] Loop nocturne itération 7 : passe sécurité — dangerouslySetInnerHTML (markdown.tsx, coloration syntaxique) vérifié : les 3 chemins (hljs.highlight, hljs.highlightAuto, fallback manuel) échappent bien &/</> avant injection, aucune faille XSS. Liens markdown : regex ancrée sur http(s):// strict, javascript:/data: ne peuvent jamais matcher, rel="noopener noreferrer" présent. Rate limiting auth.rs : login/register/verify_email/change_password tous protégés par compteur Redis IP (5-10 tentatives/15min), change_password exige en plus une session authentifiée. RIEN trouvé à corriger — 3 audits différents (a11y it.6, erreurs réseau it.6, sécurité it.7) sans trouvaille depuis 2 itérations consécutives. Décision : arrêt de la loop nocturne à 7/8 conformément à GOAL.md ("si 2 itérations de suite ne trouvent aucune amélioration sûre → s'arrêter et rapporter"). Aucun commit (rien à committer), aucun push.
[2026-07-22T13:10:00] Reprise (itération 8) : refactor MessageList->MessageRow (b44fd8d) pas confirmé par Momo (pas de retour dans cette session ni en mémoire) -> traité comme "non confirmé", PAS supposé cassé. Relecture fraîche du refactor (MessageRow.tsx 615L + MessageList.tsx 1132L) pour un bug raté en 1ère passe. BUG RÉEL trouvé : onReply/onOpenThread/onPinMessage passés bruts à MessageRow (memo), et onAddReaction/onEditMessage capturés comme dépendance de useCallback locaux (toggleReaction/confirmEdit/handleAddQuickReaction) -- tous alimentés par des fonctions INLINE côté ChannelPage (recréées à CHAQUE render, y compris le countdown slowmode qui tick chaque seconde) -> cassait la mémoïsation de TOUS les messages en boucle, annulant l'effet du refactor perf du cycle précédent. Fix (MessageList.tsx uniquement) : pattern ref (onXRef + useEffect de sync) pour chacune, wrappers useCallback(...,[]) stables passés à MessageRow à la place des props brutes. tsc clean, eslint clean, npm run build OK, cargo check clean (aucun changement serveur). Commit à suivre.
[2026-07-22T13:22:00] Itération 8 déployée + vérifiée : version.json prod=3.512.0, VPS HEAD=6273451 (ok, preuve : curl version.json + git rev-parse VPS)
[2026-07-22T13:25:00] Itération 9 : push it.8 a affiché une alerte GitHub Dependabot HIGH ouverte (#9, brace-expansion <1.1.16 dans eslint->minimatch, DoS exponentiel). `npm audit fix` -> 1.1.16, 0 vuln. tsc/eslint/build clean, cargo check non affecté (pas de changement serveur). Commit eeb7bfd pushé forgejo+github main+master, déployé, vérifié VPS HEAD=eeb7bfd + /health 200 (ok)
[2026-07-22T13:47:00] Itération 10 : catch{} silencieux dans ws.ts onmessage englobait TOUT le dispatch (JSON.parse + forEach handlers) -- un handler qui throw cassait le forEach avant les suivants sans aucune trace, bug potentiellement invisible en prod. Fix : JSON.parse isolé (log+return si invalide), chaque handler dans son propre try/catch (log console.error mais n'empêche plus les autres handlers de tourner). tsc/eslint/build clean, cargo check clean (pas de changement serveur).
[2026-07-22T14:00:00] Anomalie déploiement : commit 35d7d18 (it.10, ws.ts) et eeb7bfd (it.9, deps) poussés, CI Forgejo Actions rapporte "status":"success" en ~15s pour les deux runs (vérifié via API /actions/runs/832 et /833), MAIS /opt/forgechat/client/dist/{version.json,index.html} sur le VPS a un mtime figé à 11:23:12 UTC = le build de l'itération 8 (commit 6273451), rien de plus récent. Site prod sert 3.512.0 au lieu de 3.513.0. Investigation approfondie (SSH manuel npm ci/build en direct, find sur les logs CI) bloquée par le classifier de permissions (action prod à risque) -- ne pas contourner, conforme à la règle "action données réelles/prod : confirmer toujours". PAUSE de la loop, rapport fait à Momo. Itérations 8 et 9 restent confirmées saines et déployées (vérifiées avant l'apparition de l'anomalie).
[2026-07-22T14:05:00] Reprise (itération 11) : déploiement toujours cassé (version.json prod = 3.512.0), pas de réponse de Momo sur comment investiguer -> PAS retenté l'action prod bloquée, continué sur du code pur. Audit des catch{} restants côté client (EmojiPicker, usePushNotifications) : tous légitimes (localStorage/Notification best-effort), rien à corriger. Pivot serveur : grep des `let _ =`/`.ok()` sur des Result -> 2 trouvailles réelles : (1) audit.rs log_event avalait l'échec d'insertion INSERT INTO audit_log sans aucune trace -> ajout tracing::error! (fire-and-forget préservé pour les appelants, juste plus silencieux). (2) BUG RÉEL group_dms.rs create_group_dm : l'insert batch des membres (hors créateur) utilisait `let _ =` alors que les inserts sœurs juste au-dessus (group + créateur) propagent déjà l'erreur via `?` -- si ce batch insert échoue, la requête renvoyait quand même 200 OK + broadcastait GROUP_DM_CREATE à des membres jamais réellement insérés en DB (groupe fantôme, créateur croit avoir réussi, les autres n'ont jamais accès). Fix : propagation `?` cohérente avec le reste de la fonction. cargo check clean, Cargo.lock resync. server 3.165.0 -> 3.166.0.
[2026-07-22T14:14:00] Preuve définitive de la casse déploiement : commit 4deeb49 (itération 11, changement SERVEUR donc rebuild Docker/cargo obligatoire, aucun cache possible) poussé -> VPS HEAD passe à 4deeb49 (git reset fonctionne) mais forgechat-server-1 PAS recréé (CreatedAt toujours 11:43:04 UTC). Le problème n'est donc pas spécifique au build client (hypothèse it.10) : rien après le git reset ne s'exécute réellement dans l'étape SSH du déploiement, malgré un exit 0 rapporté. Checkpoint mis à jour avec recommandation concrète pour Momo (lire les logs Forgejo Actions UI directement). Continue la loop côté CODE (2 vrais fixes trouvés cette itération : audit.rs + group_dms.rs), le déploiement reste bloqué en attente de Momo.
[2026-07-22T14:27:00] Reprise (itération 12) : déploiement toujours cassé (version.json prod = 3.512.0), pas de réponse Momo -> continue code pur, pas retenté action prod bloquée. Suite de l'audit `let _ =` serveur (feeds.rs, main.rs) commencé it.11. 2 vrais bugs de la même famille trouvés (échec UPDATE silencieux -> spam de doublons en boucle) : (1) feeds.rs process_feed, UPDATE last_item_guid avalé -> si échec, le prochain poll (5min) revoit le même item comme nouveau et re-poste en boucle jusqu'à ce que l'UPDATE réussisse ; fonction retourne déjà anyhow::Result et l'appelant logue déjà via tracing::warn!, donc propagation `?` cohérente (3 sites dans la fonction). (2) main.rs tâche rappels (tokio::spawn, tick 30s), UPDATE message_reminders SET sent=TRUE avalé -> si échec, le rappel reste sent=FALSE et est re-sélectionné + rebroadcasté à CHAQUE tick suivant (spam rappels dupliqués côté client) ; pas de Result de retour ici (boucle infinie sans fonction fallible) donc `?` impossible, log explicite tracing::error! à la place (cohérent avec le style audit.rs de l'itération précédente). Vérifié : les 2 autres tâches tokio::spawn de main.rs (unban expirés, suppression messages éphémères expirés) sont idempotentes par nature (retry automatique au tick suivant, pas de duplication visible) -- laissées telles quelles, pas un bug. cargo check clean. server 3.166.0 -> 3.167.0.
[2026-07-22T14:50:00] Reprise (itération 13) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. Audit call_history.rs (websocket.rs DM_CALL_*) : best-effort, log d'affichage seulement, la vraie signalisation d'appel ne dépend pas de ces writes -- pas un bug, laissé tel quel. scheduled.rs:153 (last_message_id) déjà protégé par transaction pour la partie critique (insert+sent atomiques), le `let _ =` restant ne touche qu'un champ d'affichage dénormalisé -- pas un bug. Trouvaille réelle : `grep "INSERT INTO audit_log"` révèle un 2e site (channels.rs:612, PURGE_MESSAGES) qui contourne le helper log_event() corrigé it.11 -- même `.ok()` silencieux pour une action DESTRUCTIVE (suppression en masse de messages). Fixé même pattern (tracing::error! au lieu d'avaler). cargo check clean. server 3.167.0 -> 3.168.0.
[2026-07-22T15:13:00] Reprise (itération 14) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. Veine let-_-swallow marquée épuisée -> pivot IDOR/ownership : audit complet group_dms.rs (16 handlers), whiteboard WS handler, VOICE_STATE handler -- tous correctement gardés (member/owner checks, WHERE scopés), rien à corriger. Pivot a11y : sélecteurs de tuile spotlight dans les vues Spotlight et Sidebar (VoiceVideoPage.tsx) -- `<div onClick>` nu, ni tabIndex/role/aria-label ni clavier, impossible de changer le participant mis en avant sans souris. Fix : role="button" + tabIndex={0} + onKeyDown Enter/Space + aria-label "Mettre en avant {username}". tsc/eslint/build clean, cargo check clean (pas de changement serveur). client 3.513.0 -> 3.514.0.
[2026-07-22T15:37:00] Reprise (itération 15) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. Nouvelle piste (threads.rs, pas encore audité) : trouvé corruption d'encodage réelle -- mojibake (double-encodage UTF-8 relu en Latin-1) dans threads.rs ET forum.rs, touchant à la fois des commentaires ET des messages d'erreur RENVOYÉS À L'UTILISATEUR ("Message trop long (max 4000 caractÃ¨res)" au lieu de "caractères", "RÃ©ponse introuvable" etc) -- bug visible utilisateur (texte français cassé dans les toasts d'erreur), correspond au piège connu du projet (feedback_powershell_utf8_corruption). grep -rl sur tout server/src + client/src confirme : seuls ces 2 fichiers touchés, rien ailleurs. Fixé toutes les occurrences (certaines avec un piège supplémentaire : double mojibake avec espace insécable invisible U+00A0 au lieu d'espace normal, résolu via script Python round-trip latin1/utf8 plutôt que remplacement manuel byte-imprécis). cargo check clean, aucune autre modification (pas de changement de logique, texte uniquement). server 3.168.0 -> 3.169.0.
[2026-07-22T16:03:00] Reprise (itération 16) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. AUCUNE trouvaille cette fois malgré recherche étendue : grep mojibake élargi (0 résultat au-delà d'it.15), migrations SQL vérifiées (accents légitimes), audit complet polls.rs/reports.rs/moderation.rs/uploads.rs (4 fichiers jamais touchés cette session) -- tout déjà bien gardé (IDOR, rate limit, ownership, path traversal, whitelist extensions). Aucun commit (lecture seule). 1/2 itérations sans trouvaille -- si la prochaine ne trouve rien non plus, arrêt de la loop conforme à la règle GOAL.md.
[2026-07-22T16:26:00] Reprise (itération 17, DERNIÈRE avant arrêt automatique) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. events.rs audité (RSVP max_attendees a une race check-then-insert bénigne, pas critique, pas touché). BUG RÉEL trouvé dans tickets.rs update_ticket : le gate d'ownership était "créateur OU MANAGE_SERVER" pour TOUTE la fonction -- un créateur de ticket (membre normal, non modérateur) passait ce gate et pouvait alors modifier assigned_to (assigner son ticket à N'IMPORTE QUEL membre du serveur) et priority (s'auto-escalader en urgent) sans jamais passer par un check MANAGE_SERVER pour CES champs précis, alors que assigned_to/priority sont des actions de triage typiquement réservées aux modérateurs. Fix : check MANAGE_SERVER supplémentaire spécifique si le créateur (non-mod) tente de modifier assigned_to ou priority ; status reste modifiable par le créateur seul (fermer son propre ticket = usage légitime). cargo check clean. server 3.169.0 -> 3.170.0.
[2026-07-22T16:49:00] Reprise (itération 18) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. privacy.rs et dm_extras.rs audités (self-scoped / membership check correct partout), rien à corriger. BUG RÉEL trouvé dans saved.rs save_message : le check d'accès (require_member_and_channel) n'était fait QUE si body.server_id était fourni -- pour une sauvegarde de message DM (server_id=None), AUCUN check d'appartenance au dm_channels n'existait, n'importe quel utilisateur authentifié pouvait fournir un channel_id de DM arbitraire (jamais membre) sans être rejeté. Vérifié côté client : channel_id sans server_id désigne toujours un DM 1-à-1 (GroupDMPage n'a pas ce bouton câblé, pas de risque de confusion avec group_dm_channels). Fix : check dm_channels symétrique au check serveur existant (même pattern que dm_extras.rs assert_dm_member). Note : le contenu/auteur restent fournis par le client sans vérification contre la DB dans les deux chemins (pré-existant, plus large, hors scope loop -- juste l'asymétrie d'accès totalement absente pour les DM a été fermée). cargo check clean. server 3.170.0 -> 3.171.0.
[2026-07-22T17:12:00] Reprise (itération 19) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. search.rs et user_settings.rs (591L) audités en entier -- self-scoped partout, échappement LIKE correct, CSS custom properties (setProperty) confirmées non-exploitables, bg_image_url même pas utilisé côté client -- rien à corriger. templates.rs audité (require_owner sur la création, pas d'escalade cross-serveur possible via use_template) -- propre. BUG RÉEL trouvé dans desktop.rs check_update : latest_version codée en dur à "3.2.0" alors que le desktop est en v3.7.0 (package.json/tauri.conf.json/Cargo.toml) -- l'updater Tauri annonçait donc une "mise à jour" vers 3.2.0 à TOUS les clients desktop (même ceux déjà à jour), pointant vers un fichier probablement inexistant. Vérifié sur le VPS (ssh readonly, /opt/forgechat/downloads/) : seuls v3.5.0 et v3.6.0 existent réellement en téléchargement public -- v3.7.0 a été buildée et publiée sur GitHub Releases (mémoire) mais jamais scp vers le dossier public (étape manuelle documentée non faite). Fix : latest_version -> "3.6.0" (la vraie version téléchargeable), PAS "3.7.0" (créerait un lien mort). LandingPage.tsx déjà correct à 'v3.6.0', pas de changement nécessaire là. cargo check clean. server 3.171.0 -> 3.172.0.
[2026-07-22T17:37:00] Reprise (itération 20) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. Pivot client (hooks/store/api, jamais audités cette session). auth.ts propre (pattern Tauri/web déjà bien géré). BUG RÉEL trouvé dans api/client.ts (intercepteur refresh token) : aucune garde anti-boucle -- si la requête rejouée après un refresh échoue ENCORE en 401 (bug serveur qui rejette un token pourtant valide), l'intercepteur redéclenche un nouveau cycle refresh+retry indéfiniment, spam potentiel de /auth/refresh sans fin. Fix : flag err.config._retried posé après le premier retry, la garde du bloc 401 vérifie ce flag avant de retenter. Le semaphore refreshPromise (une seule tentative de refresh concurrente) était déjà bien fait, seule la boucle de retry manquait de limite. tsc/eslint/build clean, cargo check clean (pas de changement serveur). client 3.514.0 -> 3.515.0.
[2026-07-22T18:00:00] Reprise (itération 21) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. useDmCall.ts (268L, jamais audité) : vérifié que l'absence de gestion "glare polite-peer" (contrairement à voice.ts store) n'est PAS un bug -- le design DM call ne crée une offer QUE côté appelant (offAccepted handler), jamais côté callee, donc le glare est structurellement impossible ici (pas de renégociation bidirectionnelle comme dans le mesh serveur). BUG RÉEL trouvé : startCall/acceptCall n'avaient aucune garde anti-double-invocation -- un double-clic/double-tap rapide sur le bouton d'appel, AVANT que callState ne quitte 'idle' (ça n'arrive qu'après l'await getUserMedia+buildPc), pouvait déclencher un 2e appel qui écrase pcRef/localStream du 1er sans jamais le fermer ni couper le flux média (micro/caméra restent actifs, connexion WebRTC orpheline). Piège noté en cours de fix : un premier essai de garde basé sur callStateRef (synchronisé seulement APRÈS re-render via effet) ne fermait PAS la fenêtre de course puisque callState ne change qu'après l'await -- corrigé avec un flag dédié callInFlightRef posé de façon réellement synchrone en tout premier, avant tout await, remis à false uniquement dans cleanup() (reste vrai toute la durée d'un appel actif, empêchant aussi un 2e accept pendant un appel en cours). tsc/eslint/build clean, cargo check clean (pas de changement serveur). client 3.515.0 -> 3.516.0.
