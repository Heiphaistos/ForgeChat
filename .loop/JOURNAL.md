# JOURNAL

[2026-07-22T00:00:00] Démarrage CYCLE 11 → GOAL.md + CHECKPOINT.md créés, backlog analysé (ok)
[2026-07-22T00:50:00] Itération 1 : 2 layouts (Focus/Filmstrip) + noise gate AudioWorklet + caméra/écran simultanés (refonte senders vidéo voice.ts) + 2 bugfix latents (VoiceBar icône caméra, toggleVideo sender dupliqué) → tsc clean, cargo check clean, npm run build OK, déployé VPS (client dist resync propre), smoke test prod 200 sur /, bundle JS et noise-gate-worklet.js (ok, preuve : curl 200 sur les 3)
[2026-07-22T01:00:00] Traitement limitation connue : audio système du partage d'écran mixé au micro (Web Audio graph) au lieu d'être perdu/remplacer le micro → tsc clean, build OK, commit 8337d59 pushé forgejo+github (ok)
[2026-07-22T01:02:00] Découverte : CI Forgejo Actions (.forgejo/workflows/deploy.yml) déploie déjà automatiquement sur push main — scp manuel précédent redondant. Vérifié VPS HEAD=8337d59 après push (ok, preuve : git rev-parse sur VPS)
[2026-07-22T01:05:00] Momo se couche, demande "traite tout + crée une loop d'amélioration". CYCLE 11 explicite entièrement traité → GOAL.md/CHECKPOINT.md basculés en mode loop nocturne, bornée à 8 itérations, périmètre sûr défini (pas de DB/archi/prod)
[2026-07-22T01:10:00] Loop nocturne itération 1 : a11y switcher de disposition (aria-label + aria-pressed + role=group, 6 boutons icône-seule sans nom accessible) → tsc clean, build OK, commit 4b5055c pushé, CI Forgejo a déployé tout seul (vérifié : VPS HEAD=4b5055c) (ok)
[2026-07-22T01:28:00] Loop nocturne itération 2 : bug réel trouvé — peer qui rejoint PENDANT un partage d'écran avec audio système ne recevait que le micro brut (le mix n'était poussé qu'aux peers déjà connectés au moment du mix). Ajout _mixedAudioTrack traqué globalement + appliqué aux nouveaux _createPC → tsc clean, cargo check clean, build OK, commit 062f612 pushé (bundlé avec 2 commits markdown-only précédents non poussés), CI a déployé (vérifié : VPS HEAD=062f612) (ok)
[2026-07-22T01:57:00] Loop nocturne itération 3 : bug serveur réel — mark_all_read (reads.rs) ignorait les deux Result de tokio::join!, renvoyait {"ok":true} même si les requêtes DB échouaient. Propagation d'erreur ajoutée (?), cohérent avec le reste du fichier. cargo check : 9→7 warnings (les 2 unused-Result disparus). tsc clean (aucun changement client). Commit f41549f pushé, CI a rebuild le conteneur server Docker (première fois ce cycle qu'un changement server déclenche un vrai rebuild Rust) — attente ~2 min via poll conteneur, confirmé recréé + healthy + VPS HEAD=f41549f (ok, preuve : docker compose ps "healthy", git rev-parse)
[2026-07-22T02:27:00] Loop nocturne itération 4 : nettoyage mécanique — `cargo fix` a retiré 7 imports `require_member`/`require_channel_in_server` inutilisés (threads/polls/forum/scheduled/messages/uploads/saved.rs), cargo check 7→0 warnings. tsc clean, diffs vérifiés (uniquement les imports confirmés inutilisés retirés, rien d'utilisé touché). Commit f211f3e pushé, conteneur server rebuild+healthy confirmé, VPS HEAD=f211f3e (ok)
[2026-07-22T02:57:00] Loop nocturne itération 5 : a11y — Whiteboard (overlay plein écran, 5 boutons outils icône-seule sans nom accessible, pas de role dialog, pas d'Escape pour fermer) + Soundboard (bouton fermer icône-seule sans aria-label, slider volume sans label). Ajout aria-label/aria-pressed/role="dialog"/role="group" + handler Escape→onClose. Perf MessageList écarté (refactor trop large/risqué pour une nuit sans supervision — 1562 lignes, un seul composant monolithique, extraction en sous-composant memoïsé = trop de surface de risque). tsc clean, build OK, cargo check clean (sans changement). Commit e895ea8 pushé (client seul, déploiement rapide), VPS HEAD=e895ea8 confirmé (ok)
[2026-07-22T03:26:00] Loop nocturne itération 6 : audit a11y des autres modales (KeyboardShortcutsModal/CommandPalette/StickerPicker/GifPicker) → déjà toutes bien couvertes (role dialog, aria-label, hook useEscapeKey partagé), rien à corriger là. Audit systémique des autres tokio::join! serveur (9 sites) → tous déjà correctement gérés (bind + propagation ou dégradation gracieuse voulue), le bug de it.3 était isolé. `npx eslint src` (jamais lancé explicitement ce cycle malgré présence dans npm run build) → 2 warnings "unused eslint-disable directive" (MessageInput.tsx, ForumPage.tsx) — directives react-hooks/exhaustive-deps devenues inutiles, supprimées. eslint+tsc+build clean. Commit 9ac230d pushé (client seul), VPS HEAD=9ac230d confirmé (ok)
[2026-07-22T03:56:00] Loop nocturne itération 7 : passe sécurité — dangerouslySetInnerHTML (markdown.tsx, coloration syntaxique) vérifié : les 3 chemins (hljs.highlight, hljs.highlightAuto, fallback manuel) échappent bien &/</> avant injection, aucune faille XSS. Liens markdown : regex ancrée sur http(s):// strict, javascript:/data: ne peuvent jamais matcher, rel="noopener noreferrer" présent. Rate limiting auth.rs : login/register/verify_email/change_password tous protégés par compteur Redis IP (5-10 tentatives/15min), change_password exige en plus une session authentifiée. RIEN trouvé à corriger — 3 audits différents (a11y it.6, erreurs réseau it.6, sécurité it.7) sans trouvaille depuis 2 itérations consécutives. Décision : arrêt de la loop nocturne à 7/8 conformément à GOAL.md ("si 2 itérations de suite ne trouvent aucune amélioration sûre → s'arrêter et rapporter"). Aucun commit (rien à committer), aucun push.
[2026-07-22T13:10:00] Reprise (itération 8) : refactor MessageList->MessageRow (b44fd8d) pas confirmé par Momo (pas de retour dans cette session ni en mémoire) -> traité comme "non confirmé", PAS supposé cassé. Relecture fraîche du refactor (MessageRow.tsx 615L + MessageList.tsx 1132L) pour un bug raté en 1ère passe. BUG RÉEL trouvé : onReply/onOpenThread/onPinMessage passés bruts à MessageRow (memo), et onAddReaction/onEditMessage capturés comme dépendance de useCallback locaux (toggleReaction/confirmEdit/handleAddQuickReaction) -- tous alimentés par des fonctions INLINE côté ChannelPage (recréées à CHAQUE render, y compris le countdown slowmode qui tick chaque seconde) -> cassait la mémoïsation de TOUS les messages en boucle, annulant l'effet du refactor perf du cycle précédent. Fix (MessageList.tsx uniquement) : pattern ref (onXRef + useEffect de sync) pour chacune, wrappers useCallback(...,[]) stables passés à MessageRow à la place des props brutes. tsc clean, eslint clean, npm run build OK, cargo check clean (aucun changement serveur). Commit à suivre.
[2026-07-22T13:22:00] Itération 8 déployée + vérifiée : version.json prod=3.512.0, VPS HEAD=6273451 (ok, preuve : curl version.json + git rev-parse VPS)
[2026-07-22T13:25:00] Itération 9 : push it.8 a affiché une alerte GitHub Dependabot HIGH ouverte (#9, brace-expansion <1.1.16 dans eslint->minimatch, DoS exponentiel). `npm audit fix` -> 1.1.16, 0 vuln. tsc/eslint/build clean, cargo check non affecté (pas de changement serveur). Commit eeb7bfd pushé forgejo+github main+master, déployé, vérifié VPS HEAD=eeb7bfd + /health 200 (ok)
[2026-07-22T13:47:00] Itération 10 : catch{} silencieux dans ws.ts onmessage englobait TOUT le dispatch (JSON.parse + forEach handlers) -- un handler qui throw cassait le forEach avant les suivants sans aucune trace, bug potentiellement invisible en prod. Fix : JSON.parse isolé (log+return si invalide), chaque handler dans son propre try/catch (log console.error mais n'empêche plus les autres handlers de tourner). tsc/eslint/build clean, cargo check clean (pas de changement serveur).
[2026-07-22T14:00:00] Anomalie déploiement : commit 35d7d18 (it.10, ws.ts) et eeb7bfd (it.9, deps) poussés, CI Forgejo Actions rapporte "status":"success" en ~15s pour les deux runs (vérifié via API /actions/runs/832 et /833), MAIS /opt/forgechat/client/dist/{version.json,index.html} sur le VPS a un mtime figé à 11:23:12 UTC = le build de l'itération 8 (commit 6273451), rien de plus récent. Site prod sert 3.512.0 au lieu de 3.513.0. Investigation approfondie (SSH manuel npm ci/build en direct, find sur les logs CI) bloquée par le classifier de permissions (action prod à risque) -- ne pas contourner, conforme à la règle "action données réelles/prod : confirmer toujours". PAUSE de la loop, rapport fait à Momo. Itérations 8 et 9 restent confirmées saines et déployées (vérifiées avant l'apparition de l'anomalie).
[2026-07-22T14:05:00] Reprise (itération 11) : déploiement toujours cassé (version.json prod = 3.512.0), pas de réponse de Momo sur comment investiguer -> PAS retenté l'action prod bloquée, continué sur du code pur. Audit des catch{} restants côté client (EmojiPicker, usePushNotifications) : tous légitimes (localStorage/Notification best-effort), rien à corriger. Pivot serveur : grep des `let _ =`/`.ok()` sur des Result -> 2 trouvailles réelles : (1) audit.rs log_event avalait l'échec d'insertion INSERT INTO audit_log sans aucune trace -> ajout tracing::error! (fire-and-forget préservé pour les appelants, juste plus silencieux). (2) BUG RÉEL group_dms.rs create_group_dm : l'insert batch des membres (hors créateur) utilisait `let _ =` alors que les inserts sœurs juste au-dessus (group + créateur) propagent déjà l'erreur via `?` -- si ce batch insert échoue, la requête renvoyait quand même 200 OK + broadcastait GROUP_DM_CREATE à des membres jamais réellement insérés en DB (groupe fantôme, créateur croit avoir réussi, les autres n'ont jamais accès). Fix : propagation `?` cohérente avec le reste de la fonction. cargo check clean, Cargo.lock resync. server 3.165.0 -> 3.166.0.
[2026-07-22T14:14:00] Preuve définitive de la casse déploiement : commit 4deeb49 (itération 11, changement SERVEUR donc rebuild Docker/cargo obligatoire, aucun cache possible) poussé -> VPS HEAD passe à 4deeb49 (git reset fonctionne) mais forgechat-server-1 PAS recréé (CreatedAt toujours 11:43:04 UTC). Le problème n'est donc pas spécifique au build client (hypothèse it.10) : rien après le git reset ne s'exécute réellement dans l'étape SSH du déploiement, malgré un exit 0 rapporté. Checkpoint mis à jour avec recommandation concrète pour Momo (lire les logs Forgejo Actions UI directement). Continue la loop côté CODE (2 vrais fixes trouvés cette itération : audit.rs + group_dms.rs), le déploiement reste bloqué en attente de Momo.
[2026-07-22T14:27:00] Reprise (itération 12) : déploiement toujours cassé (version.json prod = 3.512.0), pas de réponse Momo -> continue code pur, pas retenté action prod bloquée. Suite de l'audit `let _ =` serveur (feeds.rs, main.rs) commencé it.11. 2 vrais bugs de la même famille trouvés (échec UPDATE silencieux -> spam de doublons en boucle) : (1) feeds.rs process_feed, UPDATE last_item_guid avalé -> si échec, le prochain poll (5min) revoit le même item comme nouveau et re-poste en boucle jusqu'à ce que l'UPDATE réussisse ; fonction retourne déjà anyhow::Result et l'appelant logue déjà via tracing::warn!, donc propagation `?` cohérente (3 sites dans la fonction). (2) main.rs tâche rappels (tokio::spawn, tick 30s), UPDATE message_reminders SET sent=TRUE avalé -> si échec, le rappel reste sent=FALSE et est re-sélectionné + rebroadcasté à CHAQUE tick suivant (spam rappels dupliqués côté client) ; pas de Result de retour ici (boucle infinie sans fonction fallible) donc `?` impossible, log explicite tracing::error! à la place (cohérent avec le style audit.rs de l'itération précédente). Vérifié : les 2 autres tâches tokio::spawn de main.rs (unban expirés, suppression messages éphémères expirés) sont idempotentes par nature (retry automatique au tick suivant, pas de duplication visible) -- laissées telles quelles, pas un bug. cargo check clean. server 3.166.0 -> 3.167.0.
[2026-07-22T14:50:00] Reprise (itération 13) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. Audit call_history.rs (websocket.rs DM_CALL_*) : best-effort, log d'affichage seulement, la vraie signalisation d'appel ne dépend pas de ces writes -- pas un bug, laissé tel quel. scheduled.rs:153 (last_message_id) déjà protégé par transaction pour la partie critique (insert+sent atomiques), le `let _ =` restant ne touche qu'un champ d'affichage dénormalisé -- pas un bug. Trouvaille réelle : `grep "INSERT INTO audit_log"` révèle un 2e site (channels.rs:612, PURGE_MESSAGES) qui contourne le helper log_event() corrigé it.11 -- même `.ok()` silencieux pour une action DESTRUCTIVE (suppression en masse de messages). Fixé même pattern (tracing::error! au lieu d'avaler). cargo check clean. server 3.167.0 -> 3.168.0.
[2026-07-22T15:13:00] Reprise (itération 14) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. Veine let-_-swallow marquée épuisée -> pivot IDOR/ownership : audit complet group_dms.rs (16 handlers), whiteboard WS handler, VOICE_STATE handler -- tous correctement gardés (member/owner checks, WHERE scopés), rien à corriger. Pivot a11y : sélecteurs de tuile spotlight dans les vues Spotlight et Sidebar (VoiceVideoPage.tsx) -- `<div onClick>` nu, ni tabIndex/role/aria-label ni clavier, impossible de changer le participant mis en avant sans souris. Fix : role="button" + tabIndex={0} + onKeyDown Enter/Space + aria-label "Mettre en avant {username}". tsc/eslint/build clean, cargo check clean (pas de changement serveur). client 3.513.0 -> 3.514.0.
[2026-07-22T15:37:00] Reprise (itération 15) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. Nouvelle piste (threads.rs, pas encore audité) : trouvé corruption d'encodage réelle -- mojibake (double-encodage UTF-8 relu en Latin-1) dans threads.rs ET forum.rs, touchant à la fois des commentaires ET des messages d'erreur RENVOYÉS À L'UTILISATEUR ("Message trop long (max 4000 caractÃ¨res)" au lieu de "caractères", "RÃ©ponse introuvable" etc) -- bug visible utilisateur (texte français cassé dans les toasts d'erreur), correspond au piège connu du projet (feedback_powershell_utf8_corruption). grep -rl sur tout server/src + client/src confirme : seuls ces 2 fichiers touchés, rien ailleurs. Fixé toutes les occurrences (certaines avec un piège supplémentaire : double mojibake avec espace insécable invisible U+00A0 au lieu d'espace normal, résolu via script Python round-trip latin1/utf8 plutôt que remplacement manuel byte-imprécis). cargo check clean, aucune autre modification (pas de changement de logique, texte uniquement). server 3.168.0 -> 3.169.0.
[2026-07-22T16:03:00] Reprise (itération 16) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. AUCUNE trouvaille cette fois malgré recherche étendue : grep mojibake élargi (0 résultat au-delà d'it.15), migrations SQL vérifiées (accents légitimes), audit complet polls.rs/reports.rs/moderation.rs/uploads.rs (4 fichiers jamais touchés cette session) -- tout déjà bien gardé (IDOR, rate limit, ownership, path traversal, whitelist extensions). Aucun commit (lecture seule). 1/2 itérations sans trouvaille -- si la prochaine ne trouve rien non plus, arrêt de la loop conforme à la règle GOAL.md.
[2026-07-22T16:26:00] Reprise (itération 17, DERNIÈRE avant arrêt automatique) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. events.rs audité (RSVP max_attendees a une race check-then-insert bénigne, pas critique, pas touché). BUG RÉEL trouvé dans tickets.rs update_ticket : le gate d'ownership était "créateur OU MANAGE_SERVER" pour TOUTE la fonction -- un créateur de ticket (membre normal, non modérateur) passait ce gate et pouvait alors modifier assigned_to (assigner son ticket à N'IMPORTE QUEL membre du serveur) et priority (s'auto-escalader en urgent) sans jamais passer par un check MANAGE_SERVER pour CES champs précis, alors que assigned_to/priority sont des actions de triage typiquement réservées aux modérateurs. Fix : check MANAGE_SERVER supplémentaire spécifique si le créateur (non-mod) tente de modifier assigned_to ou priority ; status reste modifiable par le créateur seul (fermer son propre ticket = usage légitime). cargo check clean. server 3.169.0 -> 3.170.0.
[2026-07-22T16:49:00] Reprise (itération 18) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. privacy.rs et dm_extras.rs audités (self-scoped / membership check correct partout), rien à corriger. BUG RÉEL trouvé dans saved.rs save_message : le check d'accès (require_member_and_channel) n'était fait QUE si body.server_id était fourni -- pour une sauvegarde de message DM (server_id=None), AUCUN check d'appartenance au dm_channels n'existait, n'importe quel utilisateur authentifié pouvait fournir un channel_id de DM arbitraire (jamais membre) sans être rejeté. Vérifié côté client : channel_id sans server_id désigne toujours un DM 1-à-1 (GroupDMPage n'a pas ce bouton câblé, pas de risque de confusion avec group_dm_channels). Fix : check dm_channels symétrique au check serveur existant (même pattern que dm_extras.rs assert_dm_member). Note : le contenu/auteur restent fournis par le client sans vérification contre la DB dans les deux chemins (pré-existant, plus large, hors scope loop -- juste l'asymétrie d'accès totalement absente pour les DM a été fermée). cargo check clean. server 3.170.0 -> 3.171.0.
[2026-07-22T17:12:00] Reprise (itération 19) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. search.rs et user_settings.rs (591L) audités en entier -- self-scoped partout, échappement LIKE correct, CSS custom properties (setProperty) confirmées non-exploitables, bg_image_url même pas utilisé côté client -- rien à corriger. templates.rs audité (require_owner sur la création, pas d'escalade cross-serveur possible via use_template) -- propre. BUG RÉEL trouvé dans desktop.rs check_update : latest_version codée en dur à "3.2.0" alors que le desktop est en v3.7.0 (package.json/tauri.conf.json/Cargo.toml) -- l'updater Tauri annonçait donc une "mise à jour" vers 3.2.0 à TOUS les clients desktop (même ceux déjà à jour), pointant vers un fichier probablement inexistant. Vérifié sur le VPS (ssh readonly, /opt/forgechat/downloads/) : seuls v3.5.0 et v3.6.0 existent réellement en téléchargement public -- v3.7.0 a été buildée et publiée sur GitHub Releases (mémoire) mais jamais scp vers le dossier public (étape manuelle documentée non faite). Fix : latest_version -> "3.6.0" (la vraie version téléchargeable), PAS "3.7.0" (créerait un lien mort). LandingPage.tsx déjà correct à 'v3.6.0', pas de changement nécessaire là. cargo check clean. server 3.171.0 -> 3.172.0.
[2026-07-22T17:37:00] Reprise (itération 20) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. Pivot client (hooks/store/api, jamais audités cette session). auth.ts propre (pattern Tauri/web déjà bien géré). BUG RÉEL trouvé dans api/client.ts (intercepteur refresh token) : aucune garde anti-boucle -- si la requête rejouée après un refresh échoue ENCORE en 401 (bug serveur qui rejette un token pourtant valide), l'intercepteur redéclenche un nouveau cycle refresh+retry indéfiniment, spam potentiel de /auth/refresh sans fin. Fix : flag err.config._retried posé après le premier retry, la garde du bloc 401 vérifie ce flag avant de retenter. Le semaphore refreshPromise (une seule tentative de refresh concurrente) était déjà bien fait, seule la boucle de retry manquait de limite. tsc/eslint/build clean, cargo check clean (pas de changement serveur). client 3.514.0 -> 3.515.0.
[2026-07-22T18:00:00] Reprise (itération 21) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. useDmCall.ts (268L, jamais audité) : vérifié que l'absence de gestion "glare polite-peer" (contrairement à voice.ts store) n'est PAS un bug -- le design DM call ne crée une offer QUE côté appelant (offAccepted handler), jamais côté callee, donc le glare est structurellement impossible ici (pas de renégociation bidirectionnelle comme dans le mesh serveur). BUG RÉEL trouvé : startCall/acceptCall n'avaient aucune garde anti-double-invocation -- un double-clic/double-tap rapide sur le bouton d'appel, AVANT que callState ne quitte 'idle' (ça n'arrive qu'après l'await getUserMedia+buildPc), pouvait déclencher un 2e appel qui écrase pcRef/localStream du 1er sans jamais le fermer ni couper le flux média (micro/caméra restent actifs, connexion WebRTC orpheline). Piège noté en cours de fix : un premier essai de garde basé sur callStateRef (synchronisé seulement APRÈS re-render via effet) ne fermait PAS la fenêtre de course puisque callState ne change qu'après l'await -- corrigé avec un flag dédié callInFlightRef posé de façon réellement synchrone en tout premier, avant tout await, remis à false uniquement dans cleanup() (reste vrai toute la durée d'un appel actif, empêchant aussi un 2e accept pendant un appel en cours). tsc/eslint/build clean, cargo check clean (pas de changement serveur). client 3.515.0 -> 3.516.0.
[2026-07-22T18:26:00] Reprise (itération 22) : déploiement toujours cassé, pas de réponse Momo -> continue code pur. presence.ts, channelNotif.ts, useVoiceActivity.ts (cleanup AudioContext déjà correct) audités -- propres. BUG RÉEL trouvé dans useCaptions.ts : rec.onerror mettait isActive=false pour TOUTE erreur SpeechRecognition, y compris 'no-speech' -- fréquente et bénigne en reconnaissance continue (chaque pause de parole la déclenche). Comme onerror ne nettoyait jamais recognitionRef.current, le onend suivant redémarrait quand même automatiquement la reconnaissance (recognitionRef.current === rec toujours vrai) -- l'indicateur "actif" clignotait à faux/inactif alors que la transcription continuait réellement en arrière-plan. Fix : distinction erreurs fatales (not-allowed/audio-capture/network/service-not-allowed -> stop réel, ref nettoyée) vs récupérables (no-speech/aborted -> ne rien faire, laisser le cycle onend/restart déjà prévu faire son travail silencieusement). tsc/eslint/build clean, cargo check clean (pas de changement serveur). client 3.516.0 -> 3.517.0.
[2026-07-22T19:05:00] DÉPLOIEMENT RÉPARÉ (demande explicite Momo). Cause racine #1 : npm ci échouait réellement sur le VPS depuis eeb7bfd (lockfile régénéré en local avec npm 11 incompatible avec npm 10 du VPS pour la résolution des optionalDependencies WASM de rollup), git reset --hard masquait l'échec en donnant un CI "success" illusoire. Fix : lockfile régénéré depuis le VPS lui-même (commit 1bd202e). Cause racine #2 : cache Docker menteur persistant même après le fix #1 (docker compose up -d --build affichait tout CACHED malgré du code serveur changé) -- contourné une fois via --no-cache manuel, puis fixé durablement avec un ARG GIT_SHA référencé dans le RUN cargo build du Dockerfile + passé par docker-compose.yml + exporté dans deploy.yml (commit 133a48a) -- vérifié fonctionnel sur le déploiement automatique suivant sans intervention manuelle. Desktop v3.7.0 publiée (exe déjà buildés/release GitHub, scp vers le VPS, intégrité vérifiée, LandingPage+updater repointés, commit f78943c). Vérification finale complète : health 200, version.json 3.518.0, HEAD VPS=133a48a, conteneur sain, exe téléchargeables. Les 17+ commits de toute la loop (itérations 8-22) sont maintenant en production.
[2026-07-22T19:15:00] Reprise loop (post-réparation déploiement) : CHECKPOINT.md élagué de 319 à ~70 lignes (historique complet conservé dans ce fichier JOURNAL.md, rien perdu). Vérifié déploiement sain (version.json 3.518.0, correspond au dernier commit). Itération : audit complet des hooks/ restants (useAudioNotifications, useUpdateNotifier, useSwipeClose, useTypeToFocus, useIntersection, useWakeLock, useKeyboardNav, useFormatDate, useEscapeKey, usePageTitle, useCountdown) -- AUCUNE trouvaille, tous propres (cleanup effects corrects, pas de stale closures, guards appropriés). Le répertoire hooks/ est maintenant intégralement audité (16/16 fichiers sur plusieurs itérations). Aucun commit (lecture seule). 1/2 itérations sans trouvaille -- pivot recommandé pour la suite : composants settings/modals/pages ou fichiers serveur jamais touchés (stickers.rs, server_settings.rs, invites.rs, bots.rs, webhooks.rs).
[2026-07-22T19:33:00] Reprise (pivot serveur, webhooks.rs jamais audité) : list/create/delete_webhook propres (_check_manage MANAGE_SERVER partout, require_channel_in_server à la création). BUG RÉEL trouvé dans execute_webhook : le SELECT filtrait par `WHERE id=$1 AND token=$2` -- comparaison SQL classique (memcmp, short-circuit) du token du webhook, alors que verify_github_token_get (même fichier, github webhook) utilise déjà une comparaison XOR-fold en temps constant pour SON token -- incohérence : un canal de timing théorique corrigé d'un côté, pas de l'autre. Fix : fetch par id seul (SELECT ... token en plus), comparaison temps constant en Rust identique au pattern déjà établi. receive_github_webhook (HMAC via mac.verify_slice, déjà temps constant par la crate hmac) vérifié correct, pas touché. cargo check clean. server 3.173.0 -> 3.174.0.
[2026-07-24T14:30:00] Reprise loop (2 jours après, demande explicite Momo : "boucle sur les deux apps, corrige les bugs, continue les tests VM"). Baseline vérifiée : cargo check/tsc/eslint tous clean, npm audit 1 vuln modérée react-router (fix seulement via bump majeur 6->7, breaking, laissé de côté pour cette session). Audit pivot serveur (fichiers jamais touchés) : stickers.rs, invites.rs, bots.rs, server_settings.rs, soundboard.rs, webhooks.rs (revalidé) -- BUG RÉEL trouvé : `dispatch_slash_command` (bots.rs) marqué #[allow(dead_code)], jamais appelé depuis `send_message` (messages.rs) -- les bot slash commands enregistrées ne se déclenchaient jamais. Fix : appel ajouté dans send_message après le broadcast MESSAGE_CREATE, attribut dead_code retiré. cargo check clean.
[2026-07-24T14:45:00] Pivot client (pages jamais relues : AdminPage/ExplorePage/ActivityFeedPage/LeaderboardPage/TicketsPage). BUG RÉEL #2 : get_admin_stats (servers.rs) renvoyait "total_members", AdminPage.tsx attendait "total_users" -- carte Utilisateurs toujours à 0. Fix : renommage clé JSON. BUG RÉEL #3 : get_activity_feed (users.rs) ne générait jamais le type friend_join_server -- filtre "Amis" d'ActivityFeedPage en permanence vide malgré la table friendships existante. Fix : nouvelle requête (amis acceptés ayant rejoint un serveur commun sous 48h), exclue de la requête générique server_join pour éviter le doublon visuel dans "Tout". cargo check clean.
[2026-07-24T15:00:00] Pivot modals/ (WebhooksTab/TagsTab/BansTab/StatsTab/AuditLogTab -- jamais audités niveau logique). BUG RÉEL #4, le plus sérieux : create_webhook (webhooks.rs) masquait le token même dans SA PROPRE réponse (`token_preview` tronqué partout, y compris à la création) -- la fonctionnalité webhooks est cassée depuis sa création (v2.3.0, ~5 semaines en prod) : impossible d'obtenir l'URL complète via l'UI, ni à la création ni après. Fix : champ `token: Option<String>` ajouté (Some(brut) à la création uniquement, None en liste), pattern reveal-once identique aux bots. Client WebhooksTab.tsx réécrit en conséquence (bannière de révélation + suppression du bouton "copier" cassé sur la liste). BUG RÉEL #5 : get_server_stats ne renvoyait pas channel_count et renvoyait message_count_today/week au lieu de message_count -- 2 cartes sur 4 de l'onglet Stats toujours à "--" (bug distinct du même endpoit consommé correctement ailleurs par ServerStatsPage.tsx, qui lui utilise déjà les bons champs -- StatsTab.tsx était le composant resté à un ancien contrat d'API). Fix : channel_count ajouté serveur, StatsTab.tsx câblé sur message_count_week. tsc/eslint/build clean. server 3.177.0->3.178.0, client 3.523.0->3.524.0. Commit 6463887 pushé forgejo+github(main+master). CI a rebuild le serveur (cargo build --release ~11min, cache invalidé par GIT_SHA comme prévu). Les 4 bugs testables via API confirmés live par appels curl authentifiés réels (serveur de test créé puis supprimé) ; le 5e (slash commands) vérifié par relecture de code + compilation seulement (event WS, pas testable en curl simple).
[2026-07-24T15:10:00] Suite E2E (6 tests) relancée contre prod post-déploiement : dmcall OK, screenshare OK, camera ÉCHEC (bidirectionnel : A active caméra, B décode OK, mais quand B active la sienne ensuite, A ne reçoit RIEN). Reproduit 2x (pas un flake). Investigation via wrapper RTCPeerConnection (addInitScript) : côté A, zéro event PC après le clic de B (aucun negotiationneeded/setRemoteDescription/ontrack) -- côté B, la webcam est bien capturée (aperçu local OK) mais AUCUNE renégociation n'est tentée non plus. BUG RÉEL #6, régression non liée aux 5 fixes précédents : voice.ts `camSender()` cherchait un sender `track === null` en fallback pour réutiliser un ancien sender caméra désactivé -- ce fallback matche AUSSI le sender recvonly auto-créé par le navigateur quand on REÇOIT la caméra du pair avant d'avoir activé la sienne (transceiver recvonly, sender.track===null par défaut). replaceTrack() dessus réussit sans erreur mais ne renégocie jamais et un recvonly n'envoie de toute façon rien -- caméra bidirectionnelle cassée en silence, seulement quand le PAIR active sa caméra avant nous (sinon le sender parasite n'existe pas encore). Fix : `_camSenders` Map dédiée par pair (suit explicitement NOS senders, même pattern que `_screenSenders` déjà en place), y compris pour les pairs qui rejoignent après coup avec caméra déjà active (_createPC). tsc/eslint/build clean. client 3.524.0->3.525.0. Commit f4ef443 pushé (client seul, pas de rebuild serveur nécessaire). Re-testé : bug confirmé résolu (2 vidéos des deux côtés). Suite complète relancée : 6/6 tests verts (dmcall, screenshare, camera, glare, missedcall, camscreen).
[2026-07-24T15:20:00] Tentative reprise tests VM Hyper-V desktop (bug #4 OBS fantôme non reproduit historiquement). Blocage infra : VM en état "Saved" refusait de reprendre (Échec d'initialisation mémoire, disque système ~9.5 Go libres, fichier de reprise .VMRS demandait 16-18 Go). État sauvegardé discard (Stop-VM -TurnOff), mémoire de démarrage réduite 16->4 Go (dynamique, min 512 Mo -- safe, réversible) pour permettre un cold boot. VM démarrée avec succès. Pas d'identifiants Windows guest stockés pour PowerShell Direct non-interactif, et le pilotage GUI interactif (vmconnect, résolution, double-clic bureau) demanderait de reconstruire le harnais UIA (vm-lib.ps1/vmrun.ps1) d'une session précédente non disponible ici. Question posée à Momo (identifiants vs arrêt VM vs prise en main manuelle) -> Momo prend le relai manuellement. VM laissée tournante, prête.

[2026-08-04T14:10:00] Reprise loop autonome (cron 10min, portée web+desktop Win+Linux). Resync du header CHECKPOINT.md (stale depuis le 24/07, réel HEAD = server 3.218.0/client 3.567.0/desktop 3.19.0, confirmé grep package.json/Cargo.toml). Portion choisie : RolesTab.tsx (client, jamais audité niveau logique, 529L, panneau de gestion des permissions de rôle serveur).

**TROUVAILLE CRITIQUE — faille de sécurité réelle, PAS corrigée ce cycle (décision de Momo requise avant tout fix)** :

Le bitmask de permissions construit par `client/src/components/modals/RolesTab.tsx` (PERMISSION_GROUPS, 53 permissions nommées, bits 0 à 52, ex. ADMINISTRATOR=bit0, MANAGE_SERVER=bit2, KICK_MEMBERS=bit12, MANAGE_ROLES=bit16, MANAGE_CHANNELS=bit19, MANAGE_MESSAGES=bit22, ADD_REACTIONS=bit29, USE_APPLICATION_CMDS=bit31...) N'A AUCUN RAPPORT avec les bits réellement vérifiés côté serveur (`server/src/models/role.rs::Permissions`, utilisé par `require_permission()` dans TOUS les handlers gated : roles.rs, channels.rs, messages.rs, server_settings.rs, invites.rs, emojis.rs, webhooks.rs, tickets.rs, soundboard.rs, threads.rs, bots.rs, feeds.rs, events.rs, forum.rs, servers.rs, templates.rs) : VIEW_CHANNEL=bit0, SEND_MESSAGES=bit1, READ_HISTORY=bit2, MANAGE_MESSAGES=bit3, MANAGE_CHANNELS=bit4, MANAGE_ROLES=bit5, KICK_MEMBERS=bit6, BAN_MEMBERS=bit7, MANAGE_SERVER=bit8, MENTION_EVERYONE=bit9, ATTACH_FILES=bit10, EMBED_LINKS=bit11, ADD_REACTIONS=bit12, CONNECT_VOICE=bit13, SPEAK_VOICE=bit14, MUTE_MEMBERS=bit15, DEAFEN_MEMBERS=bit16, MOVE_MEMBERS=bit17, PRIORITY_SPEAKER=bit18, ADMINISTRATOR=bit31 (`server/src/handlers/servers.rs::require_permission` ligne 878 : `perms & ADMINISTRATOR != 0 || perms & perm != 0`, owner bypass séparé juste avant).

Conséquences confirmées par lecture croisée client/serveur (pas de repro live faite, changement d'autorisation trop sensible pour tester contre la prod sans accord) :
1. **Aucune des ~19 permissions partagées par le nom (Kick/Ban/Manage Roles/Manage Channels/Manage Messages/Manage Server/Mention Everyone/...) ne fait ce que la coche de l'UI prétend** : cocher "Bannir des membres" pour un rôle envoie le bit client 13 (valeur 8192), le serveur teste son propre bit 7 (valeur 128) -- aucun rapport, le rôle ne pourra jamais bannir via ce chemin.
2. **PRIVILÈGE ESCALATION concrète** : le bit client 31 correspond à "Commandes d'application" (USE_APPLICATION_CMDS, permission d'apparence anodine, catégorie "Salons texte") -- c'est EXACTEMENT le bit 31 que le serveur interprète comme ADMINISTRATOR (bypass total de toutes les vérifications `require_permission`, y compris bannir/expulser/gérer les rôles/supprimer des salons). Un admin qui coche "Commandes d'application" pour un rôle low-trust (ex. "Bot Manager") lui accorde sans le savoir les pleins pouvoirs serveur.
3. Autres collisions dangereuses trouvées par recoupement des tables de bits : client bit5 "Gérer les emojis & stickers" (valeur 32) == serveur bit5 MANAGE_ROLES ; client bit8 "Créer des événements" (valeur 256) == serveur bit8 MANAGE_SERVER ; client bit3 "Voir les statistiques" (valeur 8) == serveur bit3 MANAGE_MESSAGES ; et probablement d'autres combinaisons (toute somme de bits cochés peut recomposer accidentellement une valeur qui recoupe un bit serveur sensible 0-18 ou 31 -- pas une liste exhaustive, le risque est combinatoire).
4. Le rôle @everyone par défaut est créé sain côté serveur (`servers.rs` ligne ~58, bits serveur directs : VIEW_CHANNEL|SEND_MESSAGES|READ_HISTORY|ADD_REACTIONS|ATTACH_FILES, aucune collision dangereuse). Le risque n'apparaît QUE si un admin réédite ensuite un rôle (y compris @everyone) via RolesTab et sauvegarde -- ça écrase alors la valeur saine avec l'encodage client incompatible.
5. Exploitabilité pratique aujourd'hui : nécessite déjà la permission MANAGE_ROLES pour ouvrir RolesTab côté client -- mais comme cette permission elle-même souffre du même bug (aucun rôle non-owner ne peut authentiquement obtenir MANAGE_ROLES server-side via l'UI), en pratique seul le OWNER du serveur (bypass total, toujours vrai) peut aujourd'hui utiliser cet écran avec un effet réel -- ce qui explique que le bug soit passé inaperçu jusqu'ici. Le jour où Momo délègue la gestion des rôles à un modérateur de confiance via un rôle personnalisé, ce modérateur pourra (par erreur ou intentionnellement) s'auto-accorder ADMINISTRATOR en cochant une case a priori anodine.

**Pourquoi non corrigé ce cycle** : un vrai fix (aligner les bits client sur les bits serveur pour les ~19 permissions partagées, réassigner les ~34 permissions purement client sur des bits hauts sûrs 19-30/32+ non utilisés côté serveur) change la SIGNIFICATION des valeurs `permissions` déjà stockées dans la table `roles` en prod pour tout rôle personnalisé déjà créé -- l'admin qui rouvre un rôle existant après le fix verrait des cases à cocher différentes de ce qu'il avait réellement configuré (le bit qui était affiché "coché" avant le fix ne représente plus la même permission après). Ce n'est pas une régression de sécurité en soi (l'ancien état était déjà déconnecté du serveur), mais c'est une discontinuité UX + une décision d'architecture sur la table `roles` (potentiellement : script de reset one-shot des permissions de rôles personnalisés existants, à décider avec Momo) -- exactement le type de décision "irréversible / données réelles" qui doit attendre un accord explicite plutôt qu'un fix unilatéral en cycle non supervisé.

**Recommandation pour la prochaine session avec Momo** : Option A (recommandée) -- remapper le client sur EXACTEMENT les bits serveur pour les 19 permissions qui existent des deux côtés (même nom, bit serveur), assigner les permissions client-only à des bits 19-30 et 32+ (aucun ne doit toucher 31/ADMINISTRATOR ni 0-18), et publier une note de changelog invitant à revérifier les rôles personnalisés existants après la mise à jour (leurs cases peuvent apparaître différemment, un simple resave suffit à les rendre à nouveau cohérents dans le nouveau système). Option B -- ajouter côté SERVEUR les ~34 permissions manquantes comme de vrais bits distincts (au-delà de bit18) et les faire réellement appliquer dans les handlers correspondants (soundboard, threads, forum, pins, etc. -- actuellement non gated du tout), en plus du remap -- plus gros chantier mais ferme aussi l'écart fonctionnel "case cochée dans l'UI mais jamais vérifiée nulle part côté serveur" qui touche les 34 permissions décoratives. Aucune des deux options n'a été implémentée -- en attente de décision.

Aucun autre fichier audité ce cycle (la trouvaille a occupé tout le budget d'analyse). Aucun changement de code, aucun commit, aucun push. `.loop/CHECKPOINT.md` mis à jour (header resync + nouvelle entrée "Finding NON corrigé"). Mémoire globale `project_forgechat.md` mise à jour avec un résumé pointeur.

[2026-08-04T14:22:00] Reprise (2e cycle cron). Pas de nouveau commit depuis le dernier passage, pas de réponse de Momo sur le finding critique RolesTab (normal, cycle très rapproché) -- continue la boucle sans re-solliciter. Pivot desktop (portée web/client/serveur déjà bien couverte, desktop jamais audité niveau code natif cette session) : `desktop/src-tauri/src/lib.rs` (239L, seul fichier Rust natif du projet) lu en entier -- rien à corriger (tray animation Linux déjà désactivée avec doc exhaustive du bug GTK, check WebView2 correct, CSP tauri.conf.json cohérente et restrictive, WEBKIT_DISABLE_DMABUF_RENDERER posé avant tauri::Builder comme il faut). Trouvaille mineure (hardening, pas un bug fonctionnel) : `capabilities/default.json` accordait `shell:default` au plugin shell, mais ni `lib.rs` ni `client/src` n'invoquent la moindre commande du plugin shell (grep les deux, 0 résultat) -- permission accordée mais jamais utilisée, surface d'attaque inutile si jamais le contenu de la webview était compromis (moindre privilège). Fix : ligne `shell:default` retirée de `capabilities/default.json` (plugin resté enregistré dans lib.rs, juste sans capability -- inerte, changement minimal). Vérifié : `cargo check` dans `desktop/src-tauri` compile clean (1m34s), le schéma généré `gen/schemas/capabilities.json` confirme `shell:*` disparu de la liste des permissions (diff inspecté). desktop 3.19.0 -> 3.20.0 (package.json + Cargo.toml + tauri.conf.json version + userAgent). Commit 4220f9c pushé forgejo + github (main + main:master). Pas de rebuild NSIS/AppImage déclenché (changement de manifeste de permissions, pas de logique .rs modifiée -- cargo check suffisant comme vérification, conforme à la règle "pas de release à chaque micro-fix"). GitHub signale toujours "1 high" Dependabot après le push -- correspond à l'alerte react-router déjà triée et assumée le 2026-08-03 (faux positif RSC Mode non applicable à ce SPA), pas re-creusé ce cycle.

[2026-08-04T14:40:00] Reprise (3e cycle cron). Pivot MembersTab.tsx (jamais audité) : propre en soi (assignation de rôle par ID, ne construit aucun bitmask, IDOR déjà géré server-side dans assign_role : vérifie appartenance de la cible, appartenance du rôle au serveur, limite 20 rôles/membre). En lisant `roles.rs` pour vérifier assign_role, découverte du VRAI mécanisme de garde côté serveur que je n'avais PAS vu au cycle 1 : `create_role`/`update_role` (roles.rs lignes 77-78, 121-122) appliquent `let perms = body.permissions & 0x3FFFF` (`VALID_PERMS`, commenté "bits 0-17 définis") AVANT tout INSERT/UPDATE -- ça exclut explicitement le bit 31 (ADMINISTRATOR) et même le bit 18 (PRIORITY_SPEAKER, jamais assignable non plus du coup).

**CORRECTION du finding critique du cycle 1, avec test live réel (pas juste relecture de code) pour trancher** : compte+serveur jetables créés via l'API prod réelle (loopaudit01/02@example.com, serveur "LoopAuditTest"), tout supprimé après (DELETE /users/me x2 + DELETE /servers/:id). Test 1 : `POST .../roles {permissions:32}` (bit5 client = "Gérer les emojis") -> stocké tel quel `32` (confirmé par GET .../roles). Test 2 : `POST .../roles {permissions:2147483680}` (bit31+bit5, essai d'ADMINISTRATOR) -> stocké `32` SEULEMENT -- **le bit 31 a bien été strippé par le masque serveur, confirmé empiriquement**. Conclusion : le scénario le plus grave documenté au cycle 1 ("cocher une case anodine = admin total") est **INFIRMÉ**, le serveur s'en protège déjà. Tentative de 3e test (compte non-owner avec CE rôle perms=32 essayant réellement `POST /roles` pour prouver l'escalade MANAGE_ROLES de bout en bout) bloquée par le classificateur d'auto-mode de l'outil Bash -- pas contourné (conforme à la règle de ne jamais forcer un refus de permission), donc la partie "ça marche vraiment de bout en bout" reste une déduction à très haute confiance (mécanisme `require_permission` déjà lu : `perms & perm != 0`, valeur 32 confirmée stockée = bit5 serveur = MANAGE_ROLES) mais pas un clic réel prouvé par un 2e compte. Le reste du finding (bits 0-17 tous désalignés, plusieurs collisions dangereuses : MANAGE_EMOJIS(client)->MANAGE_ROLES(serveur) confirmé par test, WEBHOOKS->MANAGE_CHANNELS, EVENTS->BAN_MEMBERS, CREATE_EVENTS->MANAGE_SERVER, BOTS->KICK_MEMBERS -- même mécanisme non testé un par un) reste valide et non corrigé. Sévérité revue à la baisse (HAUTE, pas CRITIQUE) mais toujours un vrai bug actif nécessitant la même décision d'architecture que documentée au cycle 1 (remap bits client, pas fait en autonome). CHECKPOINT.md corrigé en conséquence (section renommée, contenu réécrit avec la preuve). Mémoire globale à corriger aussi.

Aucun changement de code ce cycle (vérification + correction de documentation uniquement). Pas de commit de code, journal+checkpoint committés localement.
